#这些是受支持的融资模式平台github: #替换为最多4个github赞助商支持的用户名，例如[user1，user2] patreon: #替换为单个patreon用户名open_collective: #替换为单个Open Collective用户名ko_fi: #替换为单个Ko-fi用户名tidelift: #替换为单个tidelift平台名称/包名称，例如npm/babel community_bridge: #替换为单个Community Bridge项目名称，例如， cloud-foundry liberapay: #替换为单个liberapay用户名issues帐号:#替换为单个issues帐号用户名otechie: #替换为单个otechie用户名自定义:#替换为最多4个自定义赞助网址，例如['link1 '，' link2']
